# 当前核心 Mock Demo 不需要环境变量，也不读取 .env。
# 不填写 QQ、LLM 或数据库凭据即可执行 pnpm demo。
# 真实适配器的官方能力和授权确认后，再增加实际使用的配置项。

# Optional local reviewed audio (no media files are committed)
# AMP_MATERIAL_DIR=/path/to/validated/package/audio-root
# AMP_REVIEWED_MATERIALS=/path/to/intro-review.json
# AMP_AUDIO_CACHE=.local/duel-audio
# FFMPEG_PATH=ffmpeg
