# Agent Checkpoint

- Current completion status:
- Completed work:
- Remaining work:
- Branch:
- Local checkout and exact commit (if not pushed):
- PR (or reason it cannot be created):
- Tests executed and results:
- Tests not yet executed:
- Important implementation decisions:
- Known issues / risks / blockers:
- Exact recommended next steps:

Preserve changes and push a WIP commit when permitted. Prefer a Draft PR for unfinished work. Set `status:pending` and `agent:resume`, remove stale `agent:ready`/`agent:review`, and retain `agent:blocked` when necessary. The next Agent must inspect this Checkpoint, the Issue, branch, PR, commits and diff before continuing.
